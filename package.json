{
  "name": "dsa-judge",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "setup": "npm --prefix backend install && npm --prefix frontend install",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev": "concurrently --kill-others-on-fail -n api,web -c cyan,magenta \"npm run dev:backend\" \"npm run dev:frontend\"",
    "lint": "npm --prefix frontend run lint",
    "typecheck": "npm --prefix frontend run typecheck",
    "db:generate": "npm --prefix backend run prisma:generate",
    "db:push": "npm --prefix backend exec prisma db push",
    "db:seed": "npm --prefix backend run seed"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "docx": "^9.7.1"
  }
}
